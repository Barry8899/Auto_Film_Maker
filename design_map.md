# Auto Film Maker - 前端设计与架构蓝图 (Design Map)

> 状态：基础布局与UI系统确立 (Phase 2)
> 最后更新：2026-05-19

## 1. 项目概述
“Auto Film Maker”是一个自动理解视频并进行二次创作的 AI 平台。前端页面采用高度集成的 IDE / 工作站式布局，整合了 AI 智能体对话、本地文件管理与代码/文本编辑、以及视频生成生命周期追踪。

## 2. 空间布局系统 (Space Architecture)
整体视窗锁定为 **100vh** (全屏) 的桌面端工作站布局。
水平方向采用 **3 : 10 : 3** 的三栏栅格系统 (CSS Grid)：

### 2.1 左侧边栏 (L区) - 资源与历史 (占宽比 3)
垂直方向分为两部分（约 1 : 2）：
- **上部 - 对话管理 (Chat History)**：
  - “新建对话”操作区。
  - 历史对话列表卡片。
- **下部 - 工作区文件树 (File Explorer)**：
  - 对标 VSCode 的文件目录。
  - 映射路径：`/home/admin/.openclaw/workspace/auto_film_maker`

### 2.2 中间主工作区 (M区) - 核心交互区 (占宽比 10)
采用 **标签页 (Tabs)** 逻辑：
- 顶部为水平 Tabs 栏，陈列当前打开的会话或文件。
- 点击 L 区的对话或文件时，在 M 区新增或聚焦对应 Tab。
- 视窗内同一时刻仅展示当前 Active Tab 的内容（代码编辑器或 AI 聊天界面）。

### 2.3 右侧边栏 (R区) - 生产力流转条 (占宽比 3)
采用 **垂直时间线 (Vertical Timeline)** 设计：
- 8 个生命周期阶段（原片上传 -> 视频理解 -> 内容策划 -> 素材抽取 -> 分镜表生成 -> 新片段生成 -> 视频拼接 -> 成片结果）。
- 视觉呈现：左侧贯穿轴线与状态节点（打勾圆点/进行中动画），右侧辅以步骤说明文本，状态流转清晰可见。

---

## 3. 设计系统规范 (Design System Declaration)

### 3.1 视觉主题风格 (Vibe & Theme)
**"Cinematic Pro-Tool" (专业影视工作站风格)** - 默认深色模式 (Dark Mode)。
剔除廉价的炫光和渐变，保持 UI 的“后退感”，让焦点集中于中心区域的代码与内容。

### 3.2 色彩调色板 (Color Tokens)
深度融合 Vobile 品牌资产，通过 `oklch` 空间将品牌色（高饱和橙蓝）转化为适合长时间专业工作的克制色板：
- **Background (背景层)**: `oklch(25% 0.015 255)` - 提升了明度的现代微冷灰，打破沉闷感，更加通透。
- **Surface (面板层)**: `oklch(29% 0.015 255)` - L/R 区面板底色，与背景形成柔和层次。
- **Brand Blue (主辅色)**: `oklch(60% 0.12 260)` - 从 Vobile 蓝提取的温润品蓝，用于选中状态、焦点 Tab。
- **Brand Orange (语义高亮)**: `oklch(65% 0.18 45)` - 从 Vobile 橙提取的活力橙色，极度克制地仅用于：进度条的“当前进行中”节点、核心动作按钮（如“开始生成”）。
- **Text (主文本)**: `oklch(90% 0.01 250)` - 柔和白。
- **Text Muted (次文本)**: `oklch(60% 0.01 250)` - 灰阶说明。

### 3.3 排版与字体 (Typography)
- UI 界面: `Plus Jakarta Sans`
- 代码展示: `JetBrains Mono`

### 3.4 图标系统
- 统一使用基于线性的 SVG 图标（开发期使用 `[Icon]` 占位）。

---

## 4. 当前进度
- [x] LMR 布局比例与行为逻辑确认。
- [x] 主题色与排版系统确认（引入 Vobile 品牌元素）。
- [x] 生成基础 HTML v0 布局文件。
